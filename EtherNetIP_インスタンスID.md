# EtherNet/IP通信におけるインスタンスIDの役割

## 1. インスタンスIDの基本的な役割

EtherNet/IPはCIP(Common Industrial Protocol)をベースにしており、インスタンスIDはそのオブジェクトモデルの中核的な識別子です。

CIPでは通信対象を「オブジェクト」という単位で扱い、以下の階層があります。

- **クラス(Class)**:同じ種類のオブジェクトの集合(例:Assembly Object、Identity Object、Connection Objectなど)
- **インスタンス(Instance)**:クラス内の具体的な実体。インスタンスIDはこの個々の実体を一意に識別する番号
- **アトリビュート(Attribute)**:インスタンスが持つデータ項目(パラメータ値など)

つまり「クラスID + インスタンスID + アトリビュートID」の組み合わせで、通信相手のどのデータにアクセスするかを一意に特定します。

### 具体的な使われ方

- 同じクラスに複数のインスタンスが存在する場合(例:複数のI/Oポイントや複数の接続設定)、インスタンスIDでどれを指すかを区別する
- Assembly Objectでは、入力データ・出力データ・コンフィグデータなど、用途ごとに異なるインスタンスIDが割り当てられ、Implicit通信(I/Oメッセージ)でどのデータセットをやり取りするかを決める
- Explicitメッセージ(Get_Attribute_Single/Setなど)を送る際、リクエストのパスにクラスID・インスタンスID・アトリビュートIDを指定することで、対象を特定する
- インスタンスID=0はクラス自体(クラスアトリビュート)を指す特別な意味を持つことが多い

### 実務上のポイント

機器のEDS(Electronic Data Sheet)ファイルには、各Assemblyのインスタンス番号が定義されており、PLCやマスタ側の設定でこの番号を正しく指定しないと、意図したデータ(入力/出力/コンフィグ)にアクセスできません。トラブルシューティング時にまず確認すべき項目の一つです。

---

## 2. 「オブジェクト」と「コネクション」の違い

「オブジェクト」と「コネクション」は別の概念です。

### オブジェクトとは

CIPにおける「オブジェクト」は、ネットワークからアクセス可能な機能やデータの抽象的な集合体を指す、より広い概念です。CIPデバイスは複数の種類のオブジェクトの集まりとしてモデル化されています。

- **Identity Object**:デバイスの識別情報(ベンダーID、製品コードなど)
- **Assembly Object**:複数のI/Oデータをまとめて扱うためのデータ集合
- **Connection Object**:通信コネクションそのものを管理する
- **Parameter Object**:パラメータ値の管理
- (その他、機器固有のベンダー独自オブジェクトなど)

### コネクションはオブジェクトの一種

「コネクション」は、これら多数あるオブジェクトの種類の中の**一つ**である「Connection Object」に対応します。Connection Objectのインスタンスが、実際に確立された個々の通信コネクション(Implicit通信のI/O接続や、Explicitメッセージ用のコネクションなど)を表します。

### 整理表

| 概念 | 説明 |
|---|---|
| オブジェクト(全般) | CIPデバイスが持つ機能・データの単位すべて(Assembly、Identity、Connectionなど多数の種類がある) |
| Connection Object | オブジェクトの一種で、通信コネクションの管理を担当 |
| インスタンスID | どのクラス(オブジェクト種別)のオブジェクトかを問わず、そのクラス内の個々の実体を識別する番号 |

インスタンスIDは「Connection Objectのインスタンス」を指すこともあれば、「Assembly Objectのインスタンス」を指すこともあり、対象がどのクラスのオブジェクトかによって意味が変わります。

---

## 3. インスタンスIDと機器・バージョンの関係

### 正しい部分

インスタンスIDは、CIP仕様で機械的に決まる値ではなく、**各ベンダーがその機器の実装として独自に割り当てる値**です。したがって、ある機器のEDSファイルに書かれているインスタンス番号は、その機器(型式)における実装上の取り決めであり、他社の機器や別製品と共通である保証はありません。

### 注意が必要な部分

1. **「一意」の範囲**
   インスタンスIDが一意なのは「その機器の、そのオブジェクトクラス内」だけです。異なる機器同士でインスタンス番号が偶然同じ、あるいは異なるのは普通にあり得ることで、業界全体でユニークな値というわけではありません。

2. **バージョン(リビジョン)との関係**
   EDSファイルにはMajor Revision/Minor Revisionが記載されており、機器のファームウェア/ハードウェアのバージョンと対応します。
   - リビジョンが上がってもI/O構成(Assemblyの中身)が変わらなければ、インスタンス番号はそのまま維持されることが多い
   - 逆に、機能追加などでI/O構成が変更された場合は、新しいインスタンス番号が追加されたり、既存の番号の意味が変わることもある
   - これは**CIP仕様が保証しているものではなく、あくまで各ベンダーの設計方針次第**

3. **一部のオブジェクトは慣例的に固定**
   Identity Objectのインスタンス1のように、CIP仕様上「事実上の標準」として固定されているものもありますが、これは例外的なケースです。

### 実務上の結論

「型式(型番)+ そのEDSファイルが対応するリビジョン」の組み合わせで見れば、記載されているインスタンスIDは確定した値として扱えます。ただし「バージョンが変わればインスタンスIDも自動的に一意に変化する」という因果関係ではなく、あくまで**そのEDSファイル(=その時点の機器実装)に紐づいた固定値**と捉えるのが正確です。機器を選定・置き換える際は、リビジョン違いのEDSでインスタンス番号が変わっていないか、都度確認するのが安全です。

---

## 4. イメージで理解する:マンションの「部屋番号」に例える

- **クラス** = マンションの「棟」(例:A棟)
- **インスタンスID** = 各部屋の「部屋番号」(101号室、102号室…)
- **アトリビュート** = 部屋の中にある「家具」(テーブル、椅子など)

「A棟に手紙を送って」だけでは誰にも届きません。「A棟の305号室のテーブルの上に置いて」と初めて具体的な場所が決まります。インスタンスIDは、この「305号室」の部分に相当します。

### 実際のインバータ(モータドライブ)を例に

ある機器のAssembly Objectのインスタンスが、EDSファイルに以下のように定義されていたとします。

| インスタンスID | 内容 | 方向 |
|---|---|---|
| 100 | 制御指令(速度指令、start/stopビットなど) | PLC → 機器(出力) |
| 101 | ステータス情報(実速度、アラームコードなど) | 機器 → PLC(入力) |
| 102 | 拡張データ(トルク指令など) | PLC → 機器(出力) |

同じ機器の中に「制御指令用のデータ」も「ステータス用のデータ」も存在しますが、どちらも同じAssembly Objectというクラスに属しています。**もしインスタンスIDが無かったら**、機器側は「今送られてきたデータが速度指令なのか、それとも別の意味のデータなのか」を区別する手段がありません。

PLC側でI/O通信を設定するとき、「Instance 100番を使ってこのデータを送る」「Instance 101番からこのデータを受け取る」と指定することで、初めて機器はどのデータバッファを読み書きすればよいか分かります。

### まとめ

インスタンスIDの本質的な役割は、**「同じ種類の箱(クラス)がたくさんある中で、今どの箱を指しているかを区別するための番号」**です。EtherNet/IP通信の設定でインスタンス番号を間違えると、機器は正しいデータを正しい場所で読み書きできず、通信自体は繋がっても中身がデタラメになる(あるいは通信確立に失敗する)、というトラブルにつながります。
